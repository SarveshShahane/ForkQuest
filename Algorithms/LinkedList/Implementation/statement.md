Implement a linked list with following functions:

1. insertAtHead -> Takes a value as input and insert it at head position.
2. insertAtTail -> Takes a value as input and insert it at tail position.
3. deleteNode -> Takes a value and delete its first occurance from the list.
4. deleteNodebyIndex -> Takes an index and delete that index node.
5. display -> Displays the linked list

Also add some examples using the above function in main function, examples are provided (check main function).